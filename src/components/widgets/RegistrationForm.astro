---
import { Icon } from 'astro-icon/components'
---

<div class="max-w-[800px] mx-auto p-6 bg-white rounded-lg space-y-6">
  <form id="registrationForm" class="space-y-6">
    <div>
      <label for="firstName" class="block text-sm font-semibold text-gray-700 mb-2">First Name</label>
      <input 
        type="text" 
        id="firstName" 
        name="firstName" 
        required 
        class="w-full px-4 py-3 border-2 border-gray-200 rounded-lg focus:border-primary focus:ring-2 focus:ring-primary focus:ring-opacity-20 transition-colors placeholder-gray-400" 
        placeholder="Enter your first name"
      />
    </div>
    <div>
      <label for="lastName" class="block text-sm font-semibold text-gray-700 mb-2">Last Name</label>
      <input 
        type="text" 
        id="lastName" 
        name="lastName" 
        required 
        class="w-full px-4 py-3 border-2 border-gray-200 rounded-lg focus:border-primary focus:ring-2 focus:ring-primary focus:ring-opacity-20 transition-colors placeholder-gray-400" 
        placeholder="Enter your last name"
      />
    </div>
    <div>
      <label for="email" class="block text-sm font-semibold text-gray-700 mb-2">Email</label>
      <input 
        type="email" 
        id="email" 
        name="email" 
        required 
        class="w-full px-4 py-3 border-2 border-gray-200 rounded-lg focus:border-primary focus:ring-2 focus:ring-primary focus:ring-opacity-20 transition-colors placeholder-gray-400" 
        placeholder="your.email@example.com"
      />
    </div>
    <input
      type="text" 
      name="botcheck" 
      id="botcheck" 
      style="position: absolute; left: -9999px;" 
      tabindex="-1"
      autocomplete="off"
    />
    <div class="flex items-start">
      <input 
        type="checkbox" 
        id="privacy" 
        name="privacy" 
        required 
        class="mt-1 w-4 h-4 text-primary border-2 border-gray-300 rounded focus:ring-primary focus:ring-2"
      />
      <label for="privacy" class="ml-3 text-sm text-gray-700">
        I declare to have read and understood SoCraTes IT's <a href="/privacy-policy" class="text-primary hover:underline font-medium">Privacy Policy</a>
      </label>
    </div>
    <div class="space-y-2 text-sm text-gray-600 bg-gray-50 p-4 rounded-lg">
      <p>
        ✉️ I wish to receive email communications from SoCraTes IT, including news about our conferences.
      </p>
      <p>
        ℹ️ I acknowledge that my information will be transferred to Mailchimp (SoCraTes IT's emailing platform) for processing.
      </p>
    </div>
    <div id="formMessage" class="hidden">
      <div id="loadingMessage" class="hidden flex items-center justify-center text-primary">
        <svg class="animate-spin h-5 w-5 mr-3" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
          <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
          <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
        </svg>
        <span>Submitting your registration...</span>
      </div>
      <div id="errorMessage" class="hidden text-red-600 p-3 bg-red-50 rounded-lg text-sm">
        <p>There was an error submitting your registration. Please try again.</p>
      </div>
    </div>
    <button 
      type="submit" 
      id="submitBtn"
      disabled
      class="w-full bg-primary text-white font-semibold py-3 px-6 rounded-lg transition-all duration-200 disabled:bg-gray-300 disabled:cursor-not-allowed enabled:hover:bg-primary-dark enabled:hover:shadow-lg focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary"
    >
      Subscribe and start the registration process
    </button>
    <div id="turnstile-widget" class="flex justify-center my-4"></div>
  </form>
  <div class="p-6">
    <h3 class="font-bold text-lg mb-4">How the registration process works</h3>
    <ol class="list-decimal list-inside space-y-3 text-gray-700">
      <li>
        <span class="font-semibold">Fill out this form</span> and you will be redirected to the hotel booking form (Google Forms)
      </li>
      <li>
        <span class="font-semibold">Fill out the hotel form</span> and choose your preferred payment method
      </li>
      <li>
        <span class="font-semibold">Wait for payment instructions</span> from the hotel via email (within 48 hours)
      </li>
      <li>
        <span class="font-semibold">Complete your payment</span> following the hotel's instructions
      </li>
      <li>
        <span class="font-semibold">Receive confirmation</span> from the hotel
      </li>
      <li>
        <span class="font-semibold">See you in Rimini!</span> 🎉
      </li>
    </ol>
    <p class="mt-4 italic text-sm text-gray-600">
      Note: If you experience issues when subscribing using browsers with strict privacy settings, such as Firefox, 
      we recommend trying a different browser or disabling strict privacy settings temporarily.
    </p>
  </div>
  <div class="p-6">
    <div class="mx-auto space-y-4">
      <h2 class="font-bold text-lg">What is included in the cost?</h2>
      <ul class="list-disc list-inside space-y-2">
        <li>
          <strong>Two nights full-board:</strong> breakfast, lunch, dinner and snack buffet all day (vegetarian and
          gluten-free food included!)
        </li>
        <li>
          The hotel has both twin and double rooms. If there is someone you are willing to share the room with, feel
          free to book the double room
        </li>
      </ul>
    </div>

    <div class="grid md:grid-cols-2 mx-auto mt-10">
      <div class="hover:bg-subtle flex flex-col items-center justify-center">
        <h4 class="font-medium">Shared double room</h4>
        <div class="flex items-center justify-center">
          <Icon name={'single-bed'} class="size-14 text-primary" />
          <Icon name={'single-bed'} class="size-14 text-primary" />
        </div>
        <p class="mt-2 text-subtitle italic">from € 280 per attendee</p>
      </div>

      <div class="flex flex-col items-center text-center">
        <h4 class="font-medium">Single room</h4>
        <Icon name={'single-bed'} class="size-14 text-primary" />
        <p class="mt-2 text-subtitle italic">from € 360 per attendee</p>
      </div>
    </div>
  </div>
</div>

<script>
  const WEB3FORMS_ACCESS_KEY = 'ba5f906f-9b6d-4531-a91d-1c485de41b89';
  const TURNSTILE_SITE_KEY = '0x4AAAAAADWjcT1sDKbGhFHr';
  const TURNSTILE_ENABLED = true;
  
  let turnstileToken = '';
  
  const form = document.getElementById('registrationForm') as HTMLFormElement;
  const submitBtn = document.getElementById('submitBtn') as HTMLButtonElement;
  const inputs = form.querySelectorAll('input[required]');
  const formMessage = document.getElementById('formMessage') as HTMLDivElement;
  const loadingMessage = document.getElementById('loadingMessage') as HTMLDivElement;
  const errorMessage = document.getElementById('errorMessage') as HTMLDivElement;
  
  function checkFormValidity() {
    let isValid = true;
    inputs.forEach((input) => {
      const inputEl = input as HTMLInputElement;
      if (inputEl.type === 'checkbox') {
        if (!inputEl.checked) {
          isValid = false;
        }
      } else {
        if (!inputEl.value.trim()) {
          isValid = false;
        }
      }
    });
    
    submitBtn.disabled = !isValid;
  }
  
  function showLoading() {
    formMessage.classList.remove('hidden');
    loadingMessage.classList.remove('hidden');
    errorMessage.classList.add('hidden');
    submitBtn.disabled = true;
  }
  
  function showError() {
    loadingMessage.classList.add('hidden');
    errorMessage.classList.remove('hidden');
    submitBtn.disabled = false;
  }
  
  function hideMessages() {
    formMessage.classList.add('hidden');
    loadingMessage.classList.add('hidden');
    errorMessage.classList.add('hidden');
  }
  
  async function handleSubmit(e: Event) {
    e.preventDefault();
    
    const firstName = (document.getElementById('firstName') as HTMLInputElement).value;
    const lastName = (document.getElementById('lastName') as HTMLInputElement).value;
    const email = (document.getElementById('email') as HTMLInputElement).value;
    const botcheck = (document.getElementById('botcheck') as HTMLInputElement).value;
    
    if (botcheck) {
      console.warn('Bot detected');
      return;
    }
    
    if (TURNSTILE_ENABLED && !turnstileToken) {
      alert('Please complete the security check');
      return;
    }
    
    showLoading();
    
    try {
      const response = await fetch('https://api.web3forms.com/submit', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({
          access_key: WEB3FORMS_ACCESS_KEY,
          firstName: firstName,
          lastName: lastName,
          email: email,
          privacy: 'accepted',
          subject: 'New SoCraTes Italy Registration',
          botcheck: '',
        }),
      });
      
      const result = await response.json();
      
      if (result.success) {
        const googleFormUrl = new URL('https://docs.google.com/forms/d/e/1FAIpQLScytX0Px2UzTQJ1c0kHqF0fmjuI8B1n3ZbWVwxIUbX9qKqNQg/viewform');
        
        const params = new URLSearchParams({
          'entry.2139089643': firstName,
          'entry.2005620554': lastName,
          'entry.1045781291': email,
        });
        
        if (params.toString()) {
          window.location.href = `${googleFormUrl.toString()}?${params.toString()}`;
        } else {
          window.location.href = googleFormUrl.toString();
        }
      } else {
        showError();
      }
    } catch (error) {
      console.error('Error submitting form:', error);
      showError();
    }
  }
  
  inputs.forEach((input) => {
    input.addEventListener(input.type === 'checkbox' ? 'change' : 'input', checkFormValidity);
    input.addEventListener('blur', checkFormValidity);
  });
  
  form.addEventListener('submit', handleSubmit);
  
  checkFormValidity();
  
  if (TURNSTILE_ENABLED) {
    window.turnstileCallback = function() {
      turnstile.render('#turnstile-widget', {
        sitekey: TURNSTILE_SITE_KEY,
        callback: function(token: string) {
          turnstileToken = token;
        },
      });
    };
  }
</script>
<script src="https://challenges.cloudflare.com/turnstile/v0/api.js?onload=turnstileCallback" defer></script>